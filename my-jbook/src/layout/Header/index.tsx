import React from 'react';
import HeaderSection from '../../sections/Header';

const Header = () => {
    return (
        <div>
            <HeaderSection />
        </div>
    );
};

export default Header;